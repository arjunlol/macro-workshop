function foo(x$2) {
    if (!(x$2 > 42)) {
        return true;
    }
    ;
    return false;
}
y + Math.pow(x, Math.pow(10, 100)) - z;
var res = function (x$2) {
    return function (y$2) {
        return y$2 - 3;
    }(x$2 * 2);
}(10);